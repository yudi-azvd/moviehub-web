import styled from 'styled-components';

export const Container = styled.div`
  margin-bottom: 24px;

  > form {
    textarea {
      width: 100%;
      border: 1px solid #ddd;
      padding: 12px;
      border-radius: 10px;
      font-family: inherit;
    }

    button {
      /* background: #0366d6; */
    }
  }
`;
